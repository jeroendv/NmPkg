<Project DefaultTargets="Build" ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
<!-- 
!!!! AUTO-GENERATED FILE !!!!!

This file is part of the conan integration into Visual studio msbuild.
It  is auto generated by 'conanVsIntegration-integrate'

Modifying this file will result in a build failure!
-->


  <!-- Conditionally import the conanbuildinfo property sheet.
  This property sheet is dynamically generated by conan and contains all the properties needed to build the projects
  and use the conan package dependencies.

  on a clean check-out of the sourcecode this file wil not yet exists hence the need to load it conditionally.
  -->
  <Import Project="conanbuildinfo_multipleToolsets.props" Condition="exists('conanbuildinfo_multipleToolsets.props')" />

  <!-- Verify that conan integration was not corrupted
  Build fails if it is.
  -->
  <Target Name="VerifyConanTarget" BeforeTargets="ConanInstallation">
    <Message Text="Verify conanVsIntergation" Importance="high" />
    <Exec Command="ConanVsIntegration-verify -d" />
  </Target>

  <!-- install this project's conan package dependencies
  based on the active Visual Studio build settings
  -->
  <Target Name="ConanInstallation" BeforeTargets="PrepareForBuild">
    <Message Text="Install conan dependencies" Importance="high" />
    <Exec
      Command="conanVsIntegration-install -d --Platform=&quot;$(Platform)&quot; --VisualStudioVersion=&quot;$(VisualStudioVersion)&quot; --Configuration=&quot;$(Configuration)&quot; --Toolset=&quot;$(PlatformToolset)&quot; " />
  </Target>

</Project>