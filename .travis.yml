language: python
python:
  #- "2.7"
  #- "3.3"
  # - "3.4"
  # - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest --pastebin=all # or py.test for Python versions 3.5 and below
  - pytest conan_vs_integration/test/Test_Integrate.py
  - pytest -pyargs conan_vs_integration
  - pytest conan_vs_integration